// { entity, onChange }
export default function PropertyPanel() {
  // if (!entity) {
  //   return <div>未选择对象</div>;
  // }

  return (
    <div>
      {/* <h3>{entity.name}</h3>

      <div>
        类型:
        {entity.type}
      </div>

      {entity.properties.depth && (
        <>
          高度:
          <input
            value={entity.properties.depth}
            onChange={(e) => onChange("depth", Number(e.target.value))}
          />
        </>
      )} */}
    </div>
  );
}
